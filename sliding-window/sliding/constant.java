public class constant {
    public static void main(String[] args) {
        int[] arr = {1,2,4,5,6,7,8,9,3};
        int l = arr[0];
        int r = arr[arr.length-1];

        while (r<n-1){
            int sum = sum - arr[l];
            l++;
            r++;
            sum = sum + arr[r];
            int Sum = max (max sum, sum);
        }
    }
}
